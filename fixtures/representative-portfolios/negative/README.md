# Negative fixture cases

`cases.json` contains deliberate contract violations. The validator must reject every case using the exact declared error code. These are compact mutation fixtures rather than audience packages; they exist to make architecture failures executable and repeatable.
